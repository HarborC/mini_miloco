services:
  mini-miloco:
    build: .
    image: mini-miloco:local
    container_name: mini-miloco
    restart: unless-stopped
    network_mode: host
    volumes:
      - "${HOME}/.mini-miloco:/data"
